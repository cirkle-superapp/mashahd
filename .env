# Development: local SQLite (zero-cost, no external dependency)
DATABASE_URL=file:/home/z/my-project/db/custom.db

# Production: Turso (libSQL) — uncomment and replace with your Turso credentials.
# The token provided returned 401 — generate a fresh one from your Turso dashboard.
# DATABASE_URL=libsql://mashahd-fortleem.aws-us-east-1.turso.io?authToken=YOUR_FRESH_TOKEN
